const TableHeaderCell = ({ children }) => (
    <th className="py-3 px-6 text-left text-sm font-semibold text-gray-600 bg-gray-50 border-b">
        {children}
    </th>
);

export default TableHeaderCell;
