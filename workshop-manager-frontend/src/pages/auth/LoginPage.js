import React from 'react';
import LoginForm from '../../components/organism/LoginForm';

const LoginPage = () => {
    return (
        <div>
            <h1>Login to WorkshopManager</h1>
            <LoginForm />
        </div>
    );
};

export default LoginPage;
